{
  "name": "@hens/eslint-config-monorepo",
  "version": "0.1.4",
  "private": true,
  "description": "",
  "scripts": {
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "author": "hens",
  "license": "MIT",
  "devDependencies": {
    "bumpp": "^8.2.1",
    "eslint": "^8.20.0",
    "prettier": "^2.7.1"
  }
}
